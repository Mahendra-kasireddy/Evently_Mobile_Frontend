import type { JoinRole } from '../../navigation/types';

export interface RoleCardData {
  key: JoinRole;
  icon: string;
  iconColor: string;
  iconBackground: string;
  title: string;
  description: string;
  requirement: string;
}
